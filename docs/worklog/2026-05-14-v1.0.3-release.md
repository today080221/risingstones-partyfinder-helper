# v1.0.3 发布准备

## 开始状态

- 当前分支：`main`。
- 已合并 PR #4：`[codex] 修复招募列表虚拟滚动渲染异常`。
- 已删除 feature 分支：`codex/fix-virtuoso-list-rendering`。
- 已执行 `git fetch origin --tags`、`git fetch gitee --tags`。
- `HEAD`、`origin/main`、`gitee/main` 均为 `f30ddbc3d3e14eaf320c9ca39c7493ab6fabde2c`。
- 本地最新 tag 与 GitHub Release：`v1.0.2`。
- 当前 shell 未配置 `GITEE_ACCESS_TOKEN` 或 `GITEE_TOKEN`。

## 目标

- 发布 `v1.0.3`，让用户通过版本号获取招募列表重复卡片和快速滚动空白区域修复。
- 发布包继续同时内置 GitHub 与 Gitee 更新源。
- 本轮只做 release prep、版本同步、验证、打包与发布，不再修改招募筛选业务逻辑。

## 计划

- 同步 npm、Cargo、Tauri 版本号为 `1.0.3`。
- 更新 README、CHANGELOG 和 release 文档中的当前版本示例。
- 运行测试、构建、Tauri 桌面便携打包、NGA parser 校验和双源 manifest 检查。
- 提交版本同步，推送 `main` 到 GitHub/Gitee，创建并推送 `v1.0.3` tag。
- 等待 GitHub Release workflow 完成，并核验 GitHub Release 资产。

## 风险

- Gitee Release API 需要本机有效 `GITEE_ACCESS_TOKEN`；不得在仓库、日志或聊天中记录 token。
- 当前 shell 没有 Gitee token，因此 Gitee Release 资产上传若无法执行，需要记录为发布机安全 token 后补。
- Release 外层资产名继续使用英文 slug，保持旧客户端自更新兼容；zip 内主程序仍使用“阿谢姆水晶（Azem's Crystal）.exe”。

## 实现

- `package.json`、`package-lock.json` 版本同步为 `1.0.3`。
- `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 版本同步为 `1.0.3`。
- `CHANGELOG.md` 新增 `1.0.3` 用户向说明，覆盖重复卡片和快速滚动空白修复。
- README 和 release 文档中的当前包名示例更新为 `v1.0.3`。
- 保留品牌化本地产物名，同时额外准备兼容旧客户端和 README 的英文 slug Release 资产名。

## 验证

- `node --check scripts/release-names.mjs scripts/release-sources.mjs scripts/package-tauri-portable.mjs scripts/build-portable.mjs scripts/publish-gitee-release.mjs`：通过。
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`：通过。
- `npm test`：8 个测试文件、171/171 通过。
- `RISINGSTONES_REQUIRE_DUAL_UPDATE_SOURCES=true npm run release:check`：通过，包含单测、前端构建、后端打包和 Node 备用便携包生成。
- `RISINGSTONES_REQUIRE_DUAL_UPDATE_SOURCES=true npm run package:desktop:portable`：通过，生成 Tauri 桌面便携包。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：16/16 通过。
- `npm run validate:nga-parser`：curated 233/233 通过；本机样本池 688 条、688 条有正文，baseline 数量差异为非阻断提示。
- `npm run test:e2e`：10/10 通过。
- `git diff --check`：通过，仅有 Windows 行尾提示。
- 两个 zip 的 `release-manifest.json` 均为版本 `1.0.3`，且包含 `github,gitee` 双更新源；对应 `.sha256` 文件匹配。

## 本地产物

- `release/阿谢姆水晶（Azem's Crystal）-v1.0.3-desktop-win-x64-portable.zip`
- SHA256：`71B1552D04A38021628519AEB020181021491E022EA51DDCD34E5EBFA25910F7`
- `release/阿谢姆水晶（Azem's Crystal）-v1.0.3-win-x64.zip`
- SHA256：`05C674C9CA418674256A30BA0FA93EF5B70EF9F5992F576CACABB662F647DB7C`
- 兼容旧客户端的最终 Release 外层资产：
  - `risingstones-partyfinder-helper-v1.0.3-desktop-win-x64-portable.zip`
  - `risingstones-partyfinder-helper-v1.0.3-desktop-win-x64-portable.zip.sha256`
  - `risingstones-partyfinder-helper-v1.0.3-desktop-win-x64-portable.exe.sha256`
  - `risingstones-partyfinder-helper-v1.0.3-win-x64.zip`
  - `risingstones-partyfinder-helper-v1.0.3-win-x64.zip.sha256`
- 桌面 EXE SHA256：`47A5E6A293642E5F60E4F6AD52C9A4AA715FF081BDACB33E7BF7FA6DAAB8C85F`

## 发布结果

- 版本提交：`86c6ac6 发布 v1.0.3`。
- 已创建并推送 tag：`v1.0.3`，tag 指向 `86c6ac6`。
- 已推送 `main` 到 GitHub `origin` 和 Gitee `gitee`。
- GitHub Release：`https://github.com/today080221/risingstones-partyfinder-helper/releases/tag/v1.0.3`。
- GitHub Actions Release workflow：`25841239154`，结果 `success`。
- GitHub Release 已包含 Actions 生成的品牌名资产，以及 README/旧客户端兼容的 `risingstones-partyfinder-helper-v1.0.3-*` 资产。
- Gitee 仓库的 `main` 和 `v1.0.3` tag 已推送；当前 shell 未配置 `GITEE_ACCESS_TOKEN` 或 `GITEE_TOKEN`，`https://gitee.com/jianwen1126/risingstones-partyfinder-helper/releases/tag/v1.0.3` 仍返回 404，Gitee Release 页面与资产需要在安全 token 发布环境中后补。
