# v1.0.1 补丁版发布准备

## 开始状态

- 当前分支：`main`。
- 开始前 `git status --short --branch`：干净，`main` 与 `origin/main` 同步。
- 已执行 `git fetch origin` 与 `git fetch gitee`。
- `HEAD`、`origin/main`、`gitee/main` 均为 `9a3ba82b9ca09cac2e43f1084561bb3e096a5afe`。
- `v1.0.1` 本地 tag 不存在。

## 目标

- 发布 `v1.0.1`，让已拿到单源 `v1.0.0` 的客户端可以通过版本号检测到补丁更新。
- 发布包必须同时内置 GitHub 与 Gitee 更新源。
- 本轮只做 release prep、版本同步、验证、打包与发布，不修改功能代码。

## 计划

- 同步 npm、Cargo、Tauri 版本号为 `1.0.1`。
- 更新变更日志和发布文档中的当前包名示例。
- 运行测试、构建、Tauri 桌面便携打包和双源 manifest 检查。
- 提交版本同步，创建并推送 `v1.0.1` tag。
- 创建 GitHub Release，并尽可能发布 Gitee Release。

## 风险

- 同版本 SHA 变化不会触发旧客户端更新，因此必须使用 `v1.0.1` 作为补丁版本。
- Gitee Release API 需要本机有效 `GITEE_ACCESS_TOKEN`；不得在仓库或聊天中记录 token。

## 实现

- `package.json`、`package-lock.json` 版本同步为 `1.0.1`。
- `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 版本同步为 `1.0.1`。
- `CHANGELOG.md` 新增 `1.0.1` 补丁说明。
- README 和 release 文档中的当前包名示例更新为 `v1.0.1`。

## 验证

- `node --check scripts/release-names.mjs scripts/release-sources.mjs scripts/package-tauri-portable.mjs scripts/build-portable.mjs scripts/publish-gitee-release.mjs`：通过。
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`：通过。
- 发布源解析：本机解析出 `github` 和 `gitee` 两个更新源。
- `npm test`：7 个测试文件、167/167 通过。
- `npm run build`：通过。
- `npm run build:portable`：通过，生成 Node 备用便携包。
- `npm run package:desktop:portable`：通过，生成 Tauri 桌面便携包。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：16/16 通过。
- `npm run validate:nga-parser`：curated 233/233 通过，local sample pool 602 条、602 条有正文。
- `npm run test:e2e`：10/10 通过。
- 两个 zip 的 `release-manifest.json` 均包含 `github,gitee` 双更新源。

## 本地产物

- `release/阿谢姆水晶（Azem's Crystal）-v1.0.1-desktop-win-x64-portable.zip`
- SHA256：`FCBFE2EEF4ACE17945E905C815DF21CEBAB2BCB88B304044BC8E74F86FEF0289`
- `release/阿谢姆水晶（Azem's Crystal）-v1.0.1-win-x64.zip`
- SHA256：`6A8BF51181983E8D834DE5D42E4434277DFBE6B2E80283E18D37DCD407E54AC2`

## 发布结果

- 版本同步 commit：`2a038500bca8694561ebe3208501ff10cec8db7d`，提交信息为 `发布 1.0.1 双源补丁版`。
- 已创建 annotated tag：`v1.0.1`。
- 已推送：`main` 与 `v1.0.1` 到 GitHub `origin` 和 Gitee `gitee`。
- GitHub Actions Release workflow：`25627548680`，结果为 success。
- GitHub Release URL：`https://github.com/today080221/risingstones-partyfinder-helper/releases/tag/v1.0.1`。
- Gitee Release URL：`https://gitee.com/jianwen1126/risingstones-partyfinder-helper/releases/tag/v1.0.1`。
- Gitee Release id：`679717`。
- 最终外层 Release 资产使用兼容英文 slug，避免旧 `v1.0.0` 自更新校验拒绝品牌化文件名；zip 内主程序仍为 `阿谢姆水晶（Azem's Crystal）.exe`。
- 最终上传资产：
  - `risingstones-partyfinder-helper-v1.0.1-desktop-win-x64-portable.zip`
  - `risingstones-partyfinder-helper-v1.0.1-desktop-win-x64-portable.zip.sha256`
  - `risingstones-partyfinder-helper-v1.0.1-win-x64.zip`
  - `risingstones-partyfinder-helper-v1.0.1-win-x64.zip.sha256`
- 最终 SHA256：
  - `risingstones-partyfinder-helper-v1.0.1-desktop-win-x64-portable.zip`：`FCBFE2EEF4ACE17945E905C815DF21CEBAB2BCB88B304044BC8E74F86FEF0289`
  - `risingstones-partyfinder-helper-v1.0.1-win-x64.zip`：`6A8BF51181983E8D834DE5D42E4434277DFBE6B2E80283E18D37DCD407E54AC2`
- GitHub Release 资产列表已通过 `gh release view` 核验。
- GitHub Release `.sha256` 已在设置本机代理环境变量后下载核验。
- Gitee Release 详情 API 已确认 4 个发布附件均存在。
