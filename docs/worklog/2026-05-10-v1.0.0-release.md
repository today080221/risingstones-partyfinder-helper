# v1.0.0 正式版发布准备

## 开始状态

- 目标：发布 `v1.0.0`，作为首个正式版。
- 仓库：`today080221/risingstones-partyfinder-helper`。
- 当前分支：`main`。
- 预期基线：`47a65fd79fe1d9127dd42df99bd7e1497c87130b`。
- 已执行 `git fetch origin`，本地 `HEAD` 与 `origin/main` 均为 `47a65fd79fe1d9127dd42df99bd7e1497c87130b`。
- 开始时工作区干净，`v1.0.0` 本地 tag、远端 tag 和 GitHub Release 均不存在。

## 范围

- 仅做 release prep、版本号同步、验证、tag 和 GitHub Release 发布。
- 不修改功能代码。
- 不重新引入 NGA 公开页面快速读取。
- 不新增 AppAPI、浏览器伴侣或其他实验功能。

## 版本号核对

- 开始核对时：
  - `package.json`：`0.1.11`
  - `package-lock.json`：`0.1.11`
  - `src-tauri/Cargo.toml`：`0.1.11`
  - `src-tauri/Cargo.lock`：`0.1.11`
  - `src-tauri/tauri.conf.json`：`0.1.11`
- 本轮需要同步为 `1.0.0` 并提交版本同步 commit。

## 预期验证

- 必跑：
  - `npm test`
  - `npm run build`
  - `npm run desktop:build:portable`
- 时间允许时补跑完整链：
  - `cargo check --manifest-path src-tauri/Cargo.toml`
  - `cargo test --manifest-path src-tauri/Cargo.toml`
  - `npm run test:e2e`
  - `npm run validate:nga-parser`
- Tauri 配置和 Rust lockfile 版本发生变更，最终发布记录需包含 `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 或说明未运行原因。

## 风险

- 发布 tag 必须指向版本号同步后的 `main` 提交。
- Windows portable exe 必须来自当前 `v1.0.0` 构建产物。
- Release notes 需明确这是首个正式版，并保留现行安全边界：NGA 默认读取仍为用户可见 WebView 路径，不使用公开页面后台快速读取。

## 验证结果

- `npm test`：通过，7 个测试文件、167/167 tests passed。
- `npm run build`：通过，TypeScript build 与 Vite production build 完成。
- `npm run desktop:build:portable`：通过，生成 `src-tauri/target/release/risingstones-partyfinder-helper.exe`。
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，16/16 tests passed。
- `npm run test:e2e`：通过，10/10 tests passed。
- `npm run validate:nga-parser`：通过，curated 233/233；本机样本池 602/602 条含正文，默认验证只以 curated parser 断言作为失败门槛。
- `git diff --check`：通过，仅输出 Windows 工作区常见 LF/CRLF 提示。
- 静态扫尾：`src`、`src-tauri`、`scripts` 中未发现 `risingstones_nga_collect_public_samples`、`collectPublic`、`public fetch` 或 `collect_public` 等旧公开快速读取可调用入口。

## 版本提交与 Tag

- 版本同步 commit：`040a5ba8b453e7fa20871debc430462600c5d93d`，提交信息为 `同步 1.0.0 正式版版本号`。
- 版本同步范围仅包含：
  - `package.json`
  - `package-lock.json`
  - `src-tauri/Cargo.toml`
  - `src-tauri/Cargo.lock`
  - `src-tauri/tauri.conf.json`
- 已创建 annotated tag：`v1.0.0`。
- 远端 tag 剥离后指向：`040a5ba8b453e7fa20871debc430462600c5d93d`。
- 已推送：`main` 与 `v1.0.0` 到 GitHub `origin`。

## GitHub Release

- Release tag：`v1.0.0`。
- Release URL：`https://github.com/today080221/risingstones-partyfinder-helper/releases/tag/v1.0.0`。
- Release 标题：`v1.0.0 - 首个正式版`。
- Release 状态：非 draft、非 prerelease。
- 上传资产：
  - `risingstones-partyfinder-helper-v1.0.0-desktop-win-x64-portable.exe`
  - `risingstones-partyfinder-helper-v1.0.0-desktop-win-x64-portable.exe.sha256`
- Portable exe SHA256：`F956E58314DAD5A13F3AA18CEFEC430B3279C1379CA666A7FEB23716375037D5`。
- GitHub 资产 digest：`sha256:f956e58314dad5a13f3aa18cefec430b3279c1379ca666a7feb23716375037d5`。

## 剩余风险与建议

- 本次上传的是未签名 portable exe；Windows SmartScreen 拦截风险仍取决于后续代码签名和下载声誉。
- 若需要国内镜像同步，应在配置好镜像发布凭据的发布机上另行同步资产。

