# NGA 本地 WebView 聚合工作记录

> 现行产品口径已调整为“默认读取公开招募页 + 可选本机网页会话 + 可选继续浏览页辅助处理”。本文件保留 2026-05-08 当时的早期实现脉络，后续功能文档以 `docs/features/nga-public-aggregation.md` 为准。

## Start

- 开始：用户要求基于当前仓库实现 NGA 本地 WebView 聚合能力，并明确合规边界。
- 分支：`codex/nga-login-aggregation`。
- 仓库状态：切分支前 `main...origin/main` 干净；切分支后 `git status --short --branch` 显示在新分支。
- 目标：新增 Tauri 本机 WebView 入口、可选本机网页会话、可见可取消有限频采集、样本分析报告，以及统一来源卡片基础改造。

## Requirement Alignment

- 不索要账号、密码、Cookie、token 或任何认证信息。
- 登录只发生在用户本机 WebView 中。
- 保持本机网页会话只依赖 WebView 普通本地浏览器数据目录。
- 不读取、打印、导出或上传站点认证数据。
- 不绕过登录、验证码、风控、访问限制或反自动化机制。
- 最初阶段先采集样本并生成分析报告，不直接写最终 Parser 规则；本轮后续已在用户确认黑话后落地 Parser v1。
- 不确定黑话和歧义表达必须输出为待用户确认问题清单。

## Implementation Log

- 创建功能文档，现已迁移为 `docs/features/nga-public-aggregation.md`，记录合规边界、样本白名单字段、Parser 训练式工作流和验收标准。
- 在 `src/types.ts` 增加 `RecruitSource`、NGA 网页状态、采集设置、采集进度、样本、分析报告和 NGA 解析字段类型；官方招募行增加来源与解析展示的可选字段。
- 新增 `src/lib/nga.ts`，实现本机网页会话偏好解析、采集设置归一化、NGA 样本白名单清洗、取消/数量限制判断和样本分析报告生成。
- 新增 `src/lib/nga.test.ts`，覆盖本机网页会话默认关闭、风险确认分支、采集设置归一化、URL 限制、取消/数量限制、字段白名单和分析报告生成。
- 在 `src/api.ts` 增加 Tauri NGA invoke 封装；非 Tauri 运行时返回明确不可用错误。
- 在 `src/storage.ts` 保存来源筛选、NGA 采集设置和本机网页会话风险确认状态；默认保持本机网页会话关闭。
- 在 `src/App.tsx` 增加 NGA 数据源面板、本机网页会话风险提示、网页窗口打开、清除本机网页状态、可见页面采集、停止采集、样本分析报告、官方/NGA 统一卡片和“查看详情”按钮。
- 根据用户要求追加“页面就绪后读取”可见待命流程：打开 NGA WebView 后最多等待 15 分钟，按用户设置的请求间隔检查当前可见页面，识别到招募样本即停止并生成报告；停止按钮可取消待命。
- 修正 Tauri dev 端口串项目问题：本项目开发端口改为独立 `5188`，`dev:web` 固定 `--port 5188 --strictPort`，`vite.config.ts` 和 Tauri `devUrl` 同步为 `http://127.0.0.1:5188`，避免影响或误连用户正在运行的其他项目。
- 修正 NGA 页面就绪待命体验：新增 `risingstones_nga_visible_page_status`，自动待命会先判断当前窗口是否回到 NGA；非 NGA 招募页面只显示等待，不再报“当前页面不是受支持的 NGA 地址”。
- 修正 NGA 第三方登录弹窗：NGA WebView 通过 Tauri `on_new_window` 创建受控子窗口，并继承 opener 的 WebView 环境，允许登录授权流程打开新窗口。
- 修正 React StrictMode 下 Tauri invoke abort 的误报：前端把主动取消统一标记为 `AbortError`，避免显示“配置加载失败：请求已取消”。
- 根据真实 200 条样本反馈修正采集策略：列表页标题样本正文缺失率过高，因此新增“采集详情正文”选项，按请求间隔在可见 NGA WebView 中逐个打开 `read.php?tid=...` 帖子详情补齐正文。
- 增加 NGA 样本本地自动保存：保存到应用数据目录中的 `nga-samples.json`，仅保存白名单字段，多次采集按 `topicId` / URL 去重累积，最大样本池 1500 条。
- 归档用户已采集的 1500 条标题-only 样本到 `C:\Users\12553\AppData\Roaming\com.today080221.risingstones.partyfinderhelper\nga-sample-archives\nga-samples-20260508-202549-title-only.json`；确认该批样本正文数量为 0。
- 新增“补齐已存正文”：基于本地样本池已有 URL/TID，在可见 NGA WebView 中逐条打开详情页补正文；合并逻辑改为同一 `topicId` 下正文更完整的样本覆盖标题-only 样本。
- 新增 NGA 采集进度查询命令，前端在采集中轮询显示逐帖补正文进度，停止按钮仍通过同一取消状态生效。
- 增加国服、日服招募板块快捷入口：`stid=44366746` 和 `stid=42005319`。
- 根据用户指出的首页污染问题，当前页采集入口改为仅支持国服/日服招募板列表页与 `read.php?tid=...` 帖子详情页；NGA 首页、综合页、第三方登录页只显示状态，不触发当前页采集。
- 归档并清洗当前本地样本池：原 `nga-samples.json` 418 条，归档到 `C:\Users\12553\AppData\Roaming\com.today080221.risingstones.partyfinderhelper\nga-sample-archives\nga-samples-20260508-131709-before-noise-clean.json`；移除 22 条无正文首页噪音，保留 396 条全部带正文样本。
- 扩展本地样本合并清洗：无正文样本必须具备招募信号，且明显广告、公告、游戏资讯、首页推荐标题默认不进入样本池；有正文样本保留给分析报告和低置信判断。
- 修正样本报告候选提取噪声：过滤 `积极复盘/积极沟通` 被截成 `极复盘/极沟通` 的误报，避免 `D1-2` 等位置表达被当成 `1-2` 时间候选。
- 修正侧边栏易用性：侧栏加宽后继续增加右侧与底部留白、稳定滚动条 gutter，避免按钮被滚动条或底边遮挡。
- 继续加固第三方登录弹窗：主 NGA 窗口创建的二级登录弹窗也能继续创建受控三级弹窗，解决部分 OAuth/第三方登录链路被 WebView 新窗口限制卡住的问题。
- 按用户确认更新样本分析边界：`绝区零`、`游戏评测`、`版务公告` 作为噪音或非招募内容；`艾欧泽亚` 不当副本；`求职` 按玩家找队伍流处理。
- 按用户补充记录后续 Parser 词典候选：`绝妖星乱舞`、`社畜队`、`7=1`、`7=1 D1/2` 的语义进入文档草案，最终 Parser 仍需在足量详情正文样本后落地。
- 默认在平衡档隐藏疑似已招满、已关闭、广告或公告样本，但样本本身仍保留在本地样本池和分析报告中，未识别档可查看全部。
- 在 `src/styles.css` 增加 NGA 面板、来源徽标、样本报告、进度条、警告标签和解析字段样式；同时把左上角“石”字块换成更贴近 FF14/工具用途的晶石风格应用标识。
- 在 `src/lib/filters.ts` 扩展全局排除关键词匹配范围，使 NGA 原文、来源标题、解析字段和警告也能被本地筛选复用。
- 在 `src-tauri/src/main.rs` 增加 NGA WebView 命令：网页状态、打开网页窗口、清除本机网页状态、采集当前可见页面样本、取消采集。
- Tauri 采集逻辑只在用户打开的 NGA WebView 当前页面执行 DOM 文本读取，不调用 Cookie/token/localStorage/sessionStorage 读取接口，不实现自动登录或访问限制绕过。
- 新增 `@playwright/test`、`playwright.config.ts` 和 `tests/e2e/app.pw.ts`，让 UI 冒烟验证成为可重复命令。
- Playwright 默认使用自带 Chromium/headless shell；本机已用用户提供的 `chrome-headless-shell-win64.zip` 补齐 `chromium_headless_shell-1217` 缓存，并验证版本为 `Google Chrome for Testing 147.0.7727.15`。
- `.gitignore` 增加 `playwright-report/` 和 `test-results/`，避免 e2e 报告与截图附件进入仓库。
- 更新 `docs/architecture.md` 和 `docs/features/partyfinder-helper.md`，补充 NGA 本地聚合数据流、Tauri 命令、统一来源卡片和安全边界。

## Verification

- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，验证 Tauri Rust 命令层可编译。
- `npm test`：3 个测试文件、55 个测试通过，覆盖 Parser v1 的副本别名、求职流、`需求职业` 分流、已有阵容不算空缺、`8/9`、工具风险标签、`ACT/logs` 低风险、时间轴极性、反提示词、队伍社交属性、`M` 系零式、`tndd`、`edT` 边界、部队噪声、清理帖关闭、排除职业/机制位、NGA 职业/位置筛选和联系方式证据脱敏。
- `npm run test:e2e`：2 个 Playwright 测试通过，覆盖首屏、NGA 面板默认禁用状态、页面就绪后读取入口禁用状态、本机网页会话风险提示取消/确认流程。
- `npm run build`：TypeScript 与 Vite production build 通过。
- 源码级安全扫描：
  - 未发现 `document.cookie` 或 `cookies_for_url` 调用。
  - 未发现 NGA 采集代码读取 Cookie、token、localStorage、sessionStorage 或账号凭据。
  - 命中 `Cookie` / `token` 的位置为用户风险提示文案、历史安全文档、测试中的伪字段、关键词变量名或浏览器 UI 偏好存储。
- 用户已用扫码登录并采集到 200 条列表页样本；该批样本暴露出正文缺失问题，因此本轮改为支持详情正文采集和 1500 条本地累积样本池。
- 用户后续补齐正文后，本地样本池已清洗为 396 条全部带正文样本；当前报告显示标题短语型 280 条、标题分隔符结构型 94 条、固定队招募型正文 109 条、补人/替班型正文 31 条，足够支持第一轮人工规则/词典 Parser v1 设计。
- 当前样本中发现约 117 条疑似已招满或已关闭招募，平衡档默认隐藏；这些样本可用于后续实现“已招满/关闭”过滤规则。

## End

- 本轮结束状态：NGA 本地聚合基础能力和 Parser v1 已落地到 `codex/nga-login-aggregation` 分支。
- 已满足：保持本机网页会话默认关闭、首次开启风险提示、取消不开启、确认后开启、清除本机 NGA 网页状态入口、页面就绪后读取待命、采集字段白名单、可取消采集、请求间隔、最大采集数量、异常状态恢复、样本分析报告、官方卡片兼容、Parser v1 结构化字段/置信度/证据/warning/tag 输出。
- 已补充：Playwright 可用且纳入脚本，后续 UI 改动可直接运行 `npm run test:e2e`。
- 当前真实正文样本阶段结论：396 条详情正文样本可作为第一批 parser fixture；Parser v1 已能覆盖主要绝本、职位、求职、关闭、联系方式、要求和风险标签。后续仍建议补充更多楼层/多楼补充样本，因为当前详情采集主要读取主题首楼内容，可能漏掉楼主后续补充的时间变化或位置已招到信息。
- 待后续确认：单独 `第九人` 在更多样本里的语义，以及多楼更新中位置已招到的解析策略。
- Parser v1 标注阶段：用户已确认第一批词典和标签边界，包括 `绝妖星/绝凯夫卡/新绝` 映射妖星乱舞绝境战，`绝欧/TOP` 映射欧米茄绝境验证战，`绝伊甸/绝E` 大多映射光暗未来绝境战，`龙诗/DSR`、`绝亚/TEA`、`绝神兵/UWU`、`绝巴哈/UCOB` 映射对应官方绝本。
- Parser v1 标注阶段：用户已确认职业简称 `DK/黑骑`、`枪/绝枪`、`蛇`、`画`、`镰`、`盘` 的映射；其中 `盘=武士`。
- Parser v1 标注阶段：用户已确认 `绿玩/红玩/轮椅/科技/装甲车` 应作为 warning/tag 处理，不进入副本、进度或职位核心字段；正向“需要/允许/可用轮椅/科技/非绿玩”等表达记录为可能涉及第三方插件等风险，`禁止/谢绝/婉拒/非/不要` 等反提示词记录为纯净队或拒绝代打标签。
- Parser v1 标注阶段：用户已确认 `时间轴` 单独出现时不标第三方风险，可能只是官方日程、副本时间线或减伤轴；只有与 `ACT/TTS/轮椅/播报/Triggernometry/PostNamazu` 等工具词相邻时才标为 `ACT 时间轴/TTS 辅助`。
- Parser v1 标注阶段：用户已确认 `红玩绿玩均可` 可作为中性插件生态标签，`绿玩洁癖/没挂水平下降` 这类表达更像拒绝极端插件立场或插件依赖，不应误标为纯净队或队伍使用第三方风险。
- Parser v1 标注阶段：用户已确认裸 `ACT` 不能单独作为第三方风险证据；`ACT` 用于查输出、logs 记录、个人复盘时应降级为低风险要求标签。`婉拒无绘图轮椅不会打本` 表示拒绝绘图轮椅依赖，不等价于禁止所有第三方。
- Parser v1 标注阶段：用户已确认无副本关键词、无进度和无位置证据的部队/公会社交帖可作为噪声默认隐藏；只剩 `编辑+数字` 或纯数字占位的清理帖可归入已关闭流。
- Parser v1 标注阶段：用户已确认 `非绝枪MT/非蛇D1/非龙近战` 表示缺位排除职业；`D1-D4` 在招募语境下大概率表示空缺位置并按高置信处理，仅在附近有机制位/攻略位/站位等上下文时降级提示。
- Parser v1 标注阶段：用户指出 `已有/现有阵容/队伍配置` 中的位置和职业表示已占坑，不应作为空缺展示；例如标题 `7=1 D1` 与正文已有 MT/ST/H1/H2/D2/D3/D4 交叉验证时，空缺应只有 D1。
- Parser v1 标注阶段：用户已确认 logs 颜色和 `med紫/非灰0` 属于要求字段；`med` 指 median，中位数；`非灰0` 指不要 ranking 为 0 或明显被拖过本的记录。
- Parser v1 标注阶段：用户已确认 `鸟区/猫区/狗区/猪区` 分别对应陆行鸟、猫小胖、豆豆柴、莫古力；`爆肝队/时差队/休闲队/进度队/首月队/次月队` 做成队伍类型或目标标签并参与文本筛选。
- 待继续确认：若干 `8/9/第九人` 具体语义，以及多楼更新中位置已招到的解析策略。
- Thread rescue 接手：读取旧 thread handoff 和本地完整 session，确认当前工作树有意处于功能开发中，未 revert 旧改动；读取灰机 wiki HTML 时仅用脚本提取与 FF14/NGA 招募 Parser 相关的术语，不把 HTML 原文写入聊天或仓库。
- Parser v1 实现：在 `src/lib/nga.ts` 增加规则 + 词典 + 启发式分流，支持队伍招人、玩家求职、补人/替班/临时、固定队/长期队、已关闭/已招满、低置信未识别。
- Parser v1 实现：输出 `parsedFields`、`parseConfidence`、`parseEvidence`、`parseTags`、`parseWarnings`；低置信黑话不进入核心字段，`绿玩/红玩/轮椅/科技/装甲车` 只作为 requirements/warning/tag，联系方式结构化输出仅保留方法标签并对证据片段中的长数字脱敏。
- Parser v1 实现：补充 `M1S-M12S` 到阿卡迪亚登天斗技场映射、`tndd` 到 T/N + DD 职能分组、全妹队/女生优先队伍属性标签、纯净队/拒绝装甲车/第三方风险极性判断。
- Parser v1 实现：补充 `需求职业` 不触发求职流、`找齐/找到了/作废/已编辑` 等结束帖进入关闭隐藏流、已有阵容上下文不进入空缺字段、单独 `时间轴` 不进入第三方风险字段。
- Parser v1 实现：补充 `红玩绿玩均可` 中性标签、`ACT/logs 记录复盘` 低风险标签、`拒绝绘图轮椅依赖` / `拒绝插件依赖` / `拒绝极端插件立场` 反依赖标签；裸 `ACT` 不再触发 `第三方工具/插件风险`。
- Parser v1 实现：补充无副本证据的部队/公会社交帖噪声分流，以及 `编辑+数字` / 纯数字占位清理帖的关闭分流。
- Parser v1 实现：接入 NGA 卡片和详情面板，NGA 卡片展示更贴近官方招募卡片；详情显示解析状态、职业/位置、标签、警告、字段置信度和证据片段；NGA 本地筛选改为基于 Parser 位置和求职可用位置，不再把所有满编位置当作空缺。
- Parser v1 早期验证样本池：本地 `nga-samples.json` 为 396 条，396 条均有正文；当时 Parser v1 分流为队伍招人 224、玩家求职 36、已关闭 133、噪音 3、低置信未识别 0。
- Parser v1 早期覆盖率复测：全样本有效解析 396/396（100.0%），未关闭/非噪音样本 260/260（100.0%），活跃招募/求职样本 260/260（100.0%）；活跃样本至少一个高置信字段 259/260（99.6%）。
- 可读性迭代：NGA 详情从字段/证据调试视图改为“当前队伍构成、队伍详情、招募要求、攻略说明、原文参考”；来源显示改为“石之家 / NGA”，时间展示统一为 24 小时制，`10-12h`、`2-3小时` 独立为每日强度。
- 时间解析迭代：去重重复时间词和被包含子区间，识别休息日语境，避免把“周末休”“周二清 CD”等休息/活动语义混淆；跨凌晨时间显示为 `21:00-次日01:00`。
- Parser v1 迭代：标题清洗移除 `/#0amria 43205080级别: 学徒威望: 1注册: 18-08-21` 等 NGA 作者元信息，避免污染招募标题。
- Parser v1 迭代：新增 `队内配置 战骑白学僧镰诗7=1 d4` 这类连写职业顺排解析，并先按 `7=1 D4` / `6=2 H2 D1` 等空缺信息跳过空槽后再填已有职业。
- Parser v1 迭代：新增 `职业/可切职业(位置)` 写法解析，例如 `武士/蛇(d1)、舞者/机工(d3)、画家/黑魔(d4)、白魔/占(h1)、学者(h2)` 会落入对应当前队伍槽位。
- Parser v1 迭代：新增需求职业候选组解析，例如 `招募D2(侍、龙、忍、武僧)` 完整展示为武士、龙骑士、忍者、武僧；补充 `侍=武士`、`黑爷/黑黑的爷=黑魔法师`、`画家=绘灵法师`。
- UI 迭代：当前队伍构成槽位显示职业图标；多职业可切以叠层卡片展示，主职业在顶层，hover/focus 展开；招募职业候选组以同一标签内的职业小卡展示，避免丢失原始“多选一”语义。
- 2026-05-09 继续收束用户逐条标注 case：补充标题强约束优先、`几=几 + 位置` 高置信、`D1/2`/`MT/ST`/`H1/H2` 同职能可切合并，以及明确分开招 `H1 H2` 时不合并的判断。
- 2026-05-09 Parser 迭代：新增 `2T/双奶/2近` 计数职能阵容解析，支持 `2T战/dk+骑`、`双奶白/占+学`、`2近龙+镰` 等写法，避免把 `+q` 联系方式或普通加号污染职业槽位。
- 2026-05-09 Parser 迭代：新增显式空槽解析，`MT 空`、`H1 缺`、`D4 招募中` 等结构直接进入空缺位置；同时修正 `H1+D1` 这类跨职能并列位置被误合并的问题。
- 2026-05-09 Parser 迭代：强化“当前阵容”和“需求职业”分离，`已有/现有阵容/队伍配置/职业构成/阵容` 后职业不进入招募需求，`招募/需求/缺/空` 后位置才进入空缺；修正需求职业被叠到当前队伍构成的问题。
- 2026-05-09 Parser 迭代：补充 `黑白骑` 固定表达、`吃馍`、`DK` 等职业别名；单字职业连写在 `几=几` 强约束下优先按预期职业数量拆分，例如 `暗骑白学镰舞` 可拆出暗黑骑士、骑士、白魔法师、学者、钐镰客、舞者。
- 2026-05-09 Parser 迭代：排除语义改为职业优先，`非绝枪MT`、`非蛇D1` 只表达排除绝枪/蝰蛇，不排除整个 MT/D1；绝本无重复职业作为强默认筛选语义，但不硬塞进核心招募需求。
- 2026-05-09 时间迭代：统一 24 小时制，支持全角冒号、跨凌晨、晚/下午/凌晨与数字时间混写；休息日和“周二清 CD/周末休/固定休周一”反提示词用于剔除非活动日；加班、午休、每天几小时等强度单独展示。
- 2026-05-09 风险标签迭代：裸 `ACT`、`logs`、`时间轴`、`无 ACT/Dalamud 倾向`、`不装第三方` 降为中性或纯净队标签；只有明确需要/使用/依赖第三方工具、轮椅、绘图、TTS、播报等才进入风险。
- 2026-05-09 UI 迭代：NGA 详情继续按石之家卡片口径展示，增加专门“联系方式”块；区服从“大区”降权为“区服偏好”，仅当原文有跨区/指定区语义时展示；侧边栏文案改为更面向玩家的招募筛选和采集描述。
- 2026-05-09 验证脚本：新增 `npm run validate:nga-parser`，对本地 396 条正文样本做池状态和聚合统计，对用户标注 fixture 做结构化断言；脚本只打印聚合摘要，不输出原文正文。
- 2026-05-09 Parser 迭代：补充首周/次周阶段时间解析，时差队首周 `10:00-14:00 + 21:00-次日01:00` 与次周 `11:00-14:00` 分开展示；同时避免把 `3h/6-8h` 的小写 `h` 误当作 `H1/H2` 空缺。
- 2026-05-09 验证脚本：用户标注 fixture 更新为 29 个、208 个结构化断言，当前全部通过；脚本仍只打印聚合摘要，不输出原文正文。
- 2026-05-09 收尾：修正 `现招募：近战` 带冒号强提示词，空缺锁定为 `D1/D2` 并输出 `任意近战`；修正队伍构成中 `2T+H1+D1...` 的 `+` 被误当成位置可切分隔符，避免生成 `H1/D1`、`D2/D3` 假组合。
- 2026-05-09 收尾验证：`npm test` 94/94、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run test:e2e` 2/2 通过；`npm run validate:nga-parser` 显示 396/396 样本有正文，225 条高置信有效行，专项断言 208/208 通过。
