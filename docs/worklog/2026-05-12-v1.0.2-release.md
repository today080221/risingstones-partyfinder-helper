# v1.0.2 发布准备

## 开始状态

- 当前分支：`main`。
- 已合并 PR #3：`[codex] 添加标签筛选匹配模式开关`。
- 已删除 feature 分支：`codex/label-match-mode`。
- 已执行 `git fetch origin --prune` 和 `git fetch gitee`。
- `HEAD`、`origin/main`、`gitee/main` 均为 `9e28f58fa120872ca6b5a363eec5eef287f07b32`。
- 本地最新 tag：`v1.0.1`。

## 目标

- 发布 `v1.0.2`，让用户通过版本号获取标签筛选匹配模式更新。
- 发布包继续同时内置 GitHub 与 Gitee 更新源。
- 本轮只做 release prep、版本同步、验证、打包与发布，不修改 NGA、石之家、cache、parser 或 UI 之外的业务功能。

## 计划

- 同步 npm、Cargo、Tauri 版本号为 `1.0.2`。
- 更新 README、CHANGELOG 和 release 文档中的当前版本示例。
- 运行测试、构建、Tauri 桌面便携打包和双源 manifest 检查。
- 提交版本同步，创建并推送 `v1.0.2` tag。
- 创建 GitHub Release，并在本机 token 可用时发布 Gitee Release。

## 风险

- Gitee Release API 需要本机有效 `GITEE_ACCESS_TOKEN`；不得在仓库、日志或聊天中记录 token。
- Release 外层资产名继续使用英文 slug，保持旧客户端自更新兼容；zip 内主程序仍使用“阿谢姆水晶（Azem's Crystal）.exe”。

## 实现

- `package.json`、`package-lock.json` 版本同步为 `1.0.2`。
- `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 版本同步为 `1.0.2`。
- `CHANGELOG.md` 新增 `1.0.2` 用户向说明。
- README 和 release 文档中的当前包名示例更新为 `v1.0.2`。

## 验证

- `node --check scripts/release-names.mjs scripts/release-sources.mjs scripts/package-tauri-portable.mjs scripts/build-portable.mjs scripts/publish-gitee-release.mjs`：通过。
- `cargo fmt --manifest-path src-tauri/Cargo.toml --check`：通过。
- `npm test`：7 个测试文件、169/169 通过。
- `npm run build:portable`：通过，生成 Node 备用便携包；构建时强制检查双更新源。
- `npm run package:desktop:portable`：通过，生成 Tauri 桌面便携包；构建时强制检查双更新源。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：16/16 通过。
- `npm run validate:nga-parser`：curated 233/233 通过；本机样本池 649 条、649 条有正文，baseline 数量差异为非阻断提示。
- `npm run test:e2e`：10/10 通过。
- 两个 zip 的 `release-manifest.json` 均包含 `github,gitee` 双更新源。

## 本地产物

- `release/阿谢姆水晶（Azem's Crystal）-v1.0.2-desktop-win-x64-portable.zip`
- SHA256：`F8097DEC95D559B2A8E541647D1015B8C79BB0EC0F28E15691C18ED561F3C5B7`
- `release/阿谢姆水晶（Azem's Crystal）-v1.0.2-win-x64.zip`
- SHA256：`EE3B4767E9BFE7E67388BE7EF9D5D08B553092B1297076C8FDC1D8AB7F9989A8`
- 兼容旧客户端的最终 Release 外层资产：
  - `risingstones-partyfinder-helper-v1.0.2-desktop-win-x64-portable.zip`
  - `risingstones-partyfinder-helper-v1.0.2-desktop-win-x64-portable.zip.sha256`
  - `risingstones-partyfinder-helper-v1.0.2-desktop-win-x64-portable.exe.sha256`
  - `risingstones-partyfinder-helper-v1.0.2-win-x64.zip`
  - `risingstones-partyfinder-helper-v1.0.2-win-x64.zip.sha256`
- 桌面 EXE SHA256：`11B4C5C5E3ECC8758B128E75D576C293FE1B62FAA0A1ACBAD3F12B62F0B0DC6C`

## 发布结果

- 待记录。
